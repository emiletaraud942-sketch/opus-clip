// URL de l'endpoint Modal — à remplacer après `modal deploy modal_app.py`.
// Le déploiement affiche une URL du type https://<compte>--sortclip-pipeline-process.modal.run
export const MODAL_PROCESS_URL = "https://emiletaraud942-sketch--sortclip-pipeline-process.modal.run";

// Endpoint d'ajustement par texte d'un clip (même schéma d'URL que process).
// Vérifie l'URL exacte affichée par `modal deploy modal_app.py` (fonction `adjust`).
export const ADJUST_URL = "https://emiletaraud942-sketch--sortclip-pipeline-adjust.modal.run";
